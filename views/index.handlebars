<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <span class="navbar-brand">News Scraper</span>
  {{!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button> --}}
  {{!-- <div class="collapse navbar-collapse" id="navbarNav"> --}}
  <div id="navbarNav"></div>
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/saved">Saved Articles</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/scrape">Scrape New Articles</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/clear">Clear Articles</a>
      </li>
    </ul>
  </div>
</nav>

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">News Scraper</h1>
    <p class="lead">Straight from the other LA (Ruston)</p>
  </div>
</div>

<div id="container-body">
    <div id="articles">

{{#each article}}
{{#unless saved}}

<div class="card">
  <div class="card-header">
    <div id="cardTitle">{{this.title}}</div>
    <div id="saveArticleButton"><button class="btn btn-primary" id="saveButton" data-id="{{this._id}}">Save Article</button></div>
  </div>
  <div class="card-body">
    
    <p class="card-text">{{this.link}}</p>
    <p class="card-text-body">Summary: {{this.summary}}</p>
   
  </div>
</div>
{{/unless}}
{{/each}}
    </div>
</div>

